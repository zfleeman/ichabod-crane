# Email Credentials
# Fill these in with your AI's email account details
# DO NOT commit this file to a public repo!

EMAIL=your-ai@example.com
EMAIL_PASSWORD=your-email-password-or-bridge-password

# For Proton Mail Bridge (recommended):
IMAP_HOST=127.0.0.1
IMAP_PORT=1143
SMTP_HOST=127.0.0.1
SMTP_PORT=1025

# For Fastmail:
# IMAP_HOST=imap.fastmail.com
# IMAP_PORT=993
# SMTP_HOST=smtp.fastmail.com
# SMTP_PORT=587

# For Gmail (use app-specific password):
# IMAP_HOST=imap.gmail.com
# IMAP_PORT=993
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587

# Your human's name and email (so the AI knows who set it up):
HUMAN_NAME=Your Name
HUMAN_EMAIL=you@example.com
